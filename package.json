{
  "name": "shortly",
  "description": "RESTful shortlink service",
  "scripts": {
    "start": "node serve.js --port 3000 --verbose",
    "lint": "./node_modules/.bin/eslint src test serve.js",
    "pretest": "npm run --silent lint",
    "cover": "istanbul cover _mocha",
    "test": "./node_modules/.bin/mocha",
    "unit-tests": "./node_modules/.bin/mocha test/unit",
    "integration-tests": "./node_modules/.bin/mocha test/integration && pkill -f mockgoose"
  },
  "author": "Jan Heuermann",
  "license": "Apache-2.0",
  "dependencies": {
    "basic-auth": "^1.0.3",
    "express": "^4.13.4",
    "mongoose": "^4.4.2",
    "morgan": "^1.6.1",
    "random-string": "^0.1.2",
    "valid-url": "^1.0.9"
  },
  "devDependencies": {
    "codeclimate-test-reporter": "^0.3.1",
    "eslint": "^1.10.3",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5",
    "mockgoose": "^5.2.4",
    "supertest": "^1.1.0"
  }
}
